'use strict';

/**
 * available-location router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::available-location.available-location');
